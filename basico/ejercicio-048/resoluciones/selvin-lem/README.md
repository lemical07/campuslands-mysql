# Resolucion del ejercicio 048 - PRIMARY KEY para viajes y turismo

**Estudiante:** Selvin Lem  
**Nivel:** Basico retador

## Objetivo

Registrar destinos turisticos usando una clave primaria para identificar cada paquete de viaje.

## Proceso de ejecucion

Ejecutar `ddl/schema.sql`, `dml/inserts.sql` y `dql/consultas.sql` en ese orden desde MySQL.

## Decisiones tecnicas

La columna `id` es la `PRIMARY KEY` autoincremental. Las restricciones validan precio, dias y cupos disponibles.
