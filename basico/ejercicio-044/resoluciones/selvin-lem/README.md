# Resolucion del ejercicio 044 - Fechas para saga de ciencia ficcion

**Estudiante:** Selvin Lem  
**Nivel:** Basico retador

## Objetivo

Registrar peliculas y misiones de una saga de ciencia ficcion usando fechas de estreno y periodos de actividad.

## Proceso de ejecucion

Ejecutar `ddl/schema.sql`, `dml/inserts.sql` y `dql/consultas.sql` en ese orden desde MySQL.

## Decisiones tecnicas

Se usan columnas `DATE`, restricciones de presupuesto y consultas con `DATEDIFF` para medir periodos.
