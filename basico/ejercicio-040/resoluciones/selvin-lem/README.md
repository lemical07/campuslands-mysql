# Resolucion del ejercicio 040 - COUNT y SUM para carreras urbanas

**Estudiante:** Selvin Lem  
**Nivel:** Basico aplicado

## Objetivo

Registrar participantes de carreras urbanas y calcular cantidades de corredores e ingresos mediante `COUNT` y `SUM`.

## Proceso de ejecucion

Ejecutar `ddl/schema.sql`, `dml/inserts.sql` y `dql/consultas.sql` en ese orden desde MySQL.

## Decisiones tecnicas

Se usa `DECIMAL` para cuotas, `ENUM` para categorias y `CHECK` para validar distancias y pagos.
