# Resolucion del ejercicio 053 - UPDATE para arquitectura 3D

**Estudiante:** Selvin Lem  
**Nivel:** Basico retador

## Objetivo

Actualizar proyectos de arquitectura 3D mediante sentencias `UPDATE` controladas.

## Proceso de ejecucion

Ejecutar `ddl/schema.sql`, `dml/inserts.sql` y `dql/consultas.sql` en ese orden desde MySQL.

## Decisiones tecnicas

Los `UPDATE` usan condiciones especificas. Las restricciones validan presupuesto, pisos y fechas de entrega.
